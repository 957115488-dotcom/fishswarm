# IPC Contracts

Preload exposes domain-specific APIs such as `assetCenter.getSnapshot`, `assetExport.dryRun`, and `assetExport.createPackage`. Do not add generic command, fetch, file, install, run, or apply bridges for renderer use.
