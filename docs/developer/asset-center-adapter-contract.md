# Asset Center Adapter Contract

Adapters must be deterministic, read-only, path-contained, and secret-safe. They return normalized `AssetCenterItem` records with stable IDs, source references, credentialRefs instead of secrets, warnings, and safe action lists.
