# Testing

Coverage priorities:
- source adapters and normalized snapshots;
- renderer view models for Assets, Patch Review, and Export;
- planning security: secret redaction, human gate, rollback, apply baseline;
- export dry-run/package regressions;
- IPC contract tests that prevent broad command/fetch bridges.
