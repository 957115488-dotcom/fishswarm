# Memory

Memory is for stable preferences and workspace experience. Do not store secrets in memory. When using Assets, prefer structured references (`assetId`, `kind`, `title`, `summary`) over copying long or sensitive source content.
