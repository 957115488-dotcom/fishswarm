# GUI Operation

GUI operation is separate from Assets. Asset references can describe the intended workflow, but browser, desktop, and GUI actions must use dedicated guarded tools with visible state and user approval when required.
