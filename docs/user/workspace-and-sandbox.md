# Workspace and Sandbox

FishSwarm scopes file operations to the selected workspace and uses platform sandboxing where available. low-code assets remain read-only until a controlled workflow creates a patch proposal, human review gate, rollback checkpoint, and audited apply result.
