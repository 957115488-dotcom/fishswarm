# MCP Connectors

MCP server presets and declared MCP tools are indexed as read-only assets. Tool assets can be referenced in a task draft, but execution still goes through the normal MCP permission and runtime path.
