# Model Providers

Model provider assets are read-only setup references. Use Configure Provider from Assets to jump to the existing API settings page. Asset snapshots expose provider IDs and setup references only; they must not include API keys, tokens, or cookies.
